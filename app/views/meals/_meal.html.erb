<tr>
  <td width="" align="center" class="">
    <span class="list-item-link"><%= link_to 'Edit', edit_meal_path(meal.id) %></span> |
    <span class="list-item-link"><%= link_to 'Delete', meal_path(meal.id), method: :delete, data: {confirm: 'Are you sure?'} %></span> |
    <span class="list-item-link"><%= link_to 'Edit Servings', meal_path(meal.id) %></span>
  </td>
  <td width="" class="list-item-data"><%= meal.meal_date.strftime('%m/%d/%Y') %></td>
  <td width="" class="list-item-data"><%= meal.meal_type %></td>
  <td width="" align="right" class="list-item-data"><%= meal.total_calories %></td>
</tr>
